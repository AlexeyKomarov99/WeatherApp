import React from 'react';

const Pressure = ({onClick}) => {
  return (
    <section 
      className='Pressure'
      onClick={onClick}
    >
      Pressure
    </section>
  )
}

export default Pressure;